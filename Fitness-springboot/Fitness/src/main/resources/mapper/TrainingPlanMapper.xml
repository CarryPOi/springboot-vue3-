<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fitness.mapper.TrainingPlanMapper">
    <!-- 根据用户名查询训练计划列表 -->
    <select id="selectByUsername" resultType="com.example.fitness.entity.TrainingPlan">
        SELECT
            id, username, name, type, duration, frequency,
            create_time as createTime, update_time as updateTime
        FROM training_plans
        WHERE username = #{username}
        ORDER BY create_time DESC
    </select>
</mapper>